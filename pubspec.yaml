name: myapp
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cupertino_icons: ^1.0.6
  intl: ^0.19.0
  provider: ^6.1.2
  sembast: ^3.7.4
  path: ^1.9.0
  path_provider: ^2.1.4


dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_test:
    sdk: flutter

flutter_lints: ^3.0.0


flutter:
  assets:
      - assets/
  uses-material-design: true

flutter_icons:
  android: true
  ios: true
  image_path: "assets/img/icon2.png"
